{
  "name": "sum-two-largest-numbers",
  "version": "0.1.0",
  "description": "A simple experiment in summing the two largest numbers in an array",
  "repository": "https://bitbucket.org/steve-adams/sum-two-largest-numbers",
  "bugs": "https://bitbucket.org/steve-adams/sum-two-largest-numbers/issues",
  "contributors": [
    "steve@steve-adams.me"
  ],
  "main": "./src/sumTwoLargestNumbers.js",
  "author": "Steve Adams",
  "scripts": {
    "build": "tsc",
    "lint": "tslint -c tslint.json ./**/*.ts",
    "pretest": "yarn run lint",
    "test": "jest --verbose"
  },
  "devDependencies": {
    "@types/jest": "^23.3.1",
    "jest": "^23.5.0",
    "ts-jest": "^23.1.3"
  },
  "jest": {
    "testEnvironment": "node",
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "testRegex": ".spec\\.ts$"
  },
  "dependencies": {
    "typescript": "^3.0.1"
  }
}
